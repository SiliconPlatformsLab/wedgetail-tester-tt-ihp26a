// This file was autogenerated by PeakRDL-verilog
module wedgetail_spi_rf #(
    parameter                                ADDR_OFFSET = 0,  //! Module's offset in the main address map
    parameter                                ADDR_WIDTH  = 1,   //! Width of SW address bus
    parameter                                DATA_WIDTH  = 8    //! Width of SW data bus
)(
    // Clocks and resets
    input logic                              clk,     //! Default clock
    input logic                              resetn,  //! Default reset

    // Register SYS_CTRL

  /* verilator lint_off SELRANGE *//* verilator lint_on SELRANGE */
    output logic                     [ 0: 0] SYS_CTRL_RESET_q,              //! Current field value

  /* verilator lint_off SELRANGE *//* verilator lint_on SELRANGE */
    output logic                     [ 6: 0] SYS_CTRL_ECHO_q,              //! Current field value

    // Register ROSC_EN_SEL

  /* verilator lint_off SELRANGE *//* verilator lint_on SELRANGE */
    output logic                     [ 7: 0] ROSC_EN_SEL_data_q,              //! Current field value

    // Register Bus
    input  logic                             valid,    //! Active high valid
    input  logic                             read,     //! Indicates request is a read
    input  logic            [ADDR_WIDTH-1:0] addr,     //! Address (byte aligned, absolute address)
    /* verilator lint_off UNUSED */
    input  logic            [DATA_WIDTH-1:0] wdata,    //! Write data
    input  logic          [DATA_WIDTH/8-1:0] wmask,    //! Write mask
    /* verilator lint_on UNUSED */
    output logic            [DATA_WIDTH-1:0] rdata     //! Read data
);

/* verilator lint_off UNUSED */
    // local output signals for fields (unless block outputs)
    // these can be used as references in other fields
    logic                           [ 0: 0] SYS_CTRL_RESET_next;
    logic                             SYS_CTRL_RESET_anded;
    logic                             SYS_CTRL_RESET_ored;
    logic                             SYS_CTRL_RESET_xored;
    logic                           [ 6: 0] SYS_CTRL_ECHO_next;
    logic                             SYS_CTRL_ECHO_anded;
    logic                             SYS_CTRL_ECHO_ored;
    logic                             SYS_CTRL_ECHO_xored;
    logic                           [ 7: 0] ROSC_EN_SEL_data_next;
    logic                             ROSC_EN_SEL_data_anded;
    logic                             ROSC_EN_SEL_data_ored;
    logic                             ROSC_EN_SEL_data_xored;
/* verilator lint_on UNUSED */

    // ============================================================
    // SW Access logic
    // ============================================================
    logic sw_wr;
    logic sw_rd;
    /* verilator lint_off UNUSED */
    logic [DATA_WIDTH-1:0] sw_mask;
    logic [DATA_WIDTH-1:0] sw_wdata;
    logic [DATA_WIDTH-1:0] sw_rdata;
    logic [DATA_WIDTH-1:0] sw_masked_data;
    /* verilator lint_on UNUSED */

    // convert bus interface to internal sw_* signals
    assign sw_wr    = valid && !read;
    assign sw_rd    = valid &&  read;
    assign sw_wdata = wdata;
    assign rdata    = sw_rdata;

    // convert byte mask to bit mask
    always_comb begin
        int byte_idx;
        for (byte_idx = 0; byte_idx < DATA_WIDTH/8; byte_idx+=1) begin
            sw_mask[8*(byte_idx+1)-1 -: 8] = {8{wmask[byte_idx]}};
        end
    end

    // helpful masked version of data
    assign sw_masked_data = sw_wdata & sw_mask;
    logic                     [DATA_WIDTH-1:0] SYS_CTRL_rdata;
    logic                     [DATA_WIDTH-1:0] ROSC_EN_SEL_rdata;

    assign sw_rdata = // or of each register return (masked)
                   SYS_CTRL_rdata | 
                   ROSC_EN_SEL_rdata;
            
        
    // ============================================================
    // Register: SYS_CTRL
    //    [ 0: 0]                RESET: hw=r     sw=w     reset=0x0
    //    [ 7: 1]                 ECHO: hw=r     sw=rw    reset=0x3
    // ============================================================
    logic                  SYS_CTRL_decode;
    logic                  SYS_CTRL_sw_wr;
    logic                  SYS_CTRL_sw_rd;
    logic [DATA_WIDTH-1:0] SYS_CTRL_q;

    assign SYS_CTRL_decode = (addr == (ADDR_OFFSET+'h0+'h0));
    assign SYS_CTRL_sw_wr = sw_wr && SYS_CTRL_decode;
    assign SYS_CTRL_sw_rd = sw_rd && SYS_CTRL_decode;

    always_comb begin
        SYS_CTRL_q = '0;
        SYS_CTRL_q[ 0: 0] = SYS_CTRL_RESET_q;
        SYS_CTRL_q[ 7: 1] = SYS_CTRL_ECHO_q;
    end

    // masked version of return data
    assign SYS_CTRL_rdata = SYS_CTRL_sw_rd ? SYS_CTRL_q : 'b0;

    // ------------------------------------------------------------
    // Field: RESET
    // ------------------------------------------------------------
    assign SYS_CTRL_RESET_anded = & SYS_CTRL_RESET_q;
    assign SYS_CTRL_RESET_ored  = | SYS_CTRL_RESET_q;
    assign SYS_CTRL_RESET_xored = ^ SYS_CTRL_RESET_q;

    // next hardware value

    //! main storage
    always_ff @ (posedge clk, negedge resetn)
    if (~resetn) begin
        SYS_CTRL_RESET_q <= 0;
    end else begin
        // Software write
        if (SYS_CTRL_sw_wr) begin
            SYS_CTRL_RESET_q <=  sw_masked_data[ 0: 0] | (SYS_CTRL_RESET_q & ~sw_mask[ 0: 0]);
        end
    end

    // ------------------------------------------------------------
    // Field: ECHO
    // ------------------------------------------------------------
    assign SYS_CTRL_ECHO_anded = & SYS_CTRL_ECHO_q;
    assign SYS_CTRL_ECHO_ored  = | SYS_CTRL_ECHO_q;
    assign SYS_CTRL_ECHO_xored = ^ SYS_CTRL_ECHO_q;

    // next hardware value

    //! main storage
    always_ff @ (posedge clk, negedge resetn)
    if (~resetn) begin
        SYS_CTRL_ECHO_q <= 3;
    end else begin
        // Software write
        if (SYS_CTRL_sw_wr) begin
            SYS_CTRL_ECHO_q <=  sw_masked_data[ 7: 1] | (SYS_CTRL_ECHO_q & ~sw_mask[ 7: 1]);
        end
    end
            
        
    // ============================================================
    // Register: ROSC_EN_SEL
    //    [ 7: 0]                 data: hw=r     sw=rw    reset=0x0
    // ============================================================
    logic                  ROSC_EN_SEL_decode;
    logic                  ROSC_EN_SEL_sw_wr;
    logic                  ROSC_EN_SEL_sw_rd;
    logic [DATA_WIDTH-1:0] ROSC_EN_SEL_q;

    assign ROSC_EN_SEL_decode = (addr == (ADDR_OFFSET+'h0+'h1));
    assign ROSC_EN_SEL_sw_wr = sw_wr && ROSC_EN_SEL_decode;
    assign ROSC_EN_SEL_sw_rd = sw_rd && ROSC_EN_SEL_decode;

    always_comb begin
        ROSC_EN_SEL_q = '0;
        ROSC_EN_SEL_q[ 7: 0] = ROSC_EN_SEL_data_q;
    end

    // masked version of return data
    assign ROSC_EN_SEL_rdata = ROSC_EN_SEL_sw_rd ? ROSC_EN_SEL_q : 'b0;

    // ------------------------------------------------------------
    // Field: data
    // ------------------------------------------------------------
    assign ROSC_EN_SEL_data_anded = & ROSC_EN_SEL_data_q;
    assign ROSC_EN_SEL_data_ored  = | ROSC_EN_SEL_data_q;
    assign ROSC_EN_SEL_data_xored = ^ ROSC_EN_SEL_data_q;

    // next hardware value

    //! main storage
    always_ff @ (posedge clk, negedge resetn)
    if (~resetn) begin
        ROSC_EN_SEL_data_q <= 0;
    end else begin
        // Software write
        if (ROSC_EN_SEL_sw_wr) begin
            ROSC_EN_SEL_data_q <=  sw_masked_data[ 7: 0] | (ROSC_EN_SEL_data_q & ~sw_mask[ 7: 0]);
        end
    end

endmodule: wedgetail_spi_rf
